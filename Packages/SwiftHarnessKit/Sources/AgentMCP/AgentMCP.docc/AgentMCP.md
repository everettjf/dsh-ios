# ``AgentMCP``

Connect Model Context Protocol servers to Swift Harness Kit.

``MCPClient`` discovers remote tools and ``MCPTool`` adapts them to the native
agent tool contract. Hosts can use ``URLSessionMCPTransport`` or provide a custom
``MCPTransport``.
