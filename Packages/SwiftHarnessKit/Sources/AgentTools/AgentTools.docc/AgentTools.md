# ``AgentTools``

Define, register, authorize, and audit agent tools.

``AgentTool`` is the executable tool contract and ``ToolRegistry`` supplies
definitions and invocation to the runtime. Use ``GovernedTool`` with a
``ToolAuthorizationPolicy`` for permission-sensitive operations.
