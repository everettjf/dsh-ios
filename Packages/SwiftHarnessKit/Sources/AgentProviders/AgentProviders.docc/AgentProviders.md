# ``AgentProviders``

Connect Swift Harness Kit to streaming model APIs.

Use ``OpenAICompatibleProvider`` for DeepSeek and OpenAI-compatible chat
completion endpoints. ``SSEDecoder`` exposes the incremental event decoder for
custom transports and tests.
